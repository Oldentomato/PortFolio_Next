---
title: pip설치
date: 2022-01-18
description: 파이썬, pip
---

- 윈도우에서 pip를 설치하려면 관리자모드로 cmd를 켠후,  
```console
curl https://bootstrap.pypa.io/get-pip.py -o get-pip.py

를 입력하여 다운을 받고 그 뒤에,  
```console
python get-pip.py (파이썬이 설치된것을 가정하에)

를 입력하여 설치하면 된다.