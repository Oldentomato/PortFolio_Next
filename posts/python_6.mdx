---
title: enumerate() 함수
date: 2022-06-22
description: 파이썬 문법 중 enumerate에 관한 설명
tag: [파이썬, 문법]
---

인덱스와 원소를 동시에 접근하면서 루프를 돌릴 때 쓰는 함수이다.
이 함수는 기본적으로 인덱스와 원소로 이루어진 튜플(tuple)을 만들어준다.
따라서 기본적으로 인덱스와 원소를 각각 다른 변수에 할당하고 싶다면 인자풀기를 해줘야한다.
```python
for i , letter in enumerate([배열]):
  print(i, letter) # i가 인덱스(숫자) letter가 원소
```
파라미터로 start를 넣으면 해당 인덱스부터 (start)를 읽게 된다.
ex) enumerate([배열], start=2)
